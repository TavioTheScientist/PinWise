include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
  errors:
    # The Swift core is exhaustively documented; keep the Dart port honest too.
    public_member_api_docs: ignore
